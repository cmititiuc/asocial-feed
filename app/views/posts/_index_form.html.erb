<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :topic_id, [['-- None --', '']] + current_user.topics.order(:name).map{ |t| [t.name, t.id] } %>
  </div>
  <div class="field">
    <%= f.text_area :body, :style=>'width: 100%; height: 100%' %>
  </div>
  <%= hidden_field_tag :return_to, posts_path %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
